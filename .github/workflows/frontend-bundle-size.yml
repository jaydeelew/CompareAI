name: Frontend Bundle Size Check

on:
  pull_request:
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-bundle-size.yml'
  push:
    branches:
      - main
      - develop
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-bundle-size.yml'

jobs:
  bundle-size:
    name: Check Bundle Size
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci
      
      - name: Build application
        working-directory: ./frontend
        run: npm run build
      
      - name: Check bundle size
        working-directory: ./frontend
        run: npm run bundle:size
      
      - name: Upload bundle analysis
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bundle-analysis
          path: frontend/dist/stats.html
          retention-days: 7
      
      - name: Comment bundle size on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            
            // Try to read bundle size info from build output
            // This is a placeholder - you can enhance this to parse actual bundle sizes
            const comment = `## 📦 Bundle Size Check
            
            Bundle size check completed. View the detailed analysis in the workflow artifacts.
            
            **Next steps:**
            - Download \`bundle-analysis\` artifact to view \`stats.html\`
            - Review bundle size limits in \`frontend/scripts/check-bundle-size.js\`
            - Optimize imports and remove unused dependencies if limits are exceeded
            `;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

